import { getUstensilList } from '../utils/getUstensilList.js';

/**
 * Select ustensil tag in dropdown menu
 * @param {Array} recipes 
 */
export function selectUstensil(recipes) {
  const array = getUstensilList(recipes);
}